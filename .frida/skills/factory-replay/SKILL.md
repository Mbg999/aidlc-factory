---
name: factory-replay
description: Re-run an AIDLC orchestrator run from a specific stage. Rolls the manifest back, archives output handoffs, and routes to the chosen stage.
---

Adopt the role and authority rules from `.claude/agents/orchestrator.md`.

**Arguments:** `<run-id> --from <stage-name>` provided by the user.

Read and execute the full sequence from `.aidlc-orchestrator/runtime/cmd-factory-replay.md`.

Hard rules from `.claude/agents/orchestrator.md` apply.
