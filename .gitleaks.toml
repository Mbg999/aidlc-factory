# Gitleaks configuration
# https://github.com/gitleaks/gitleaks#configuration

title = "aidlc-workflows gitleaks config"

[extend]
# Extend the default ruleset (don't replace it)
useDefault = true

[allowlist]
description = "Global allowlist"
paths = [
    '''\.gitleaks-baseline\.json$''',
    '''uv\.lock$''',
    # Credential scrubber/tests intentionally contain fake credentials as fixtures
    '''test_credential_scrubber\.py$''',
    '''test_factory_secretscan\.py$''',
]
