# Minimal dependencies for skills installer and factory scripts
pyyaml>=6.0
jsonschema>=4.0

# Optional developer/test tools
pytest>=7.0

# boto3 is only required by aidlc-scripts/aidlc-evaluator/ — see its pyproject.toml
